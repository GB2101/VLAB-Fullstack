# V-Lab Full Stack Challenge (Solicitações de Atendimento)

Stack: **React + TypeScript** · **PHP/Laravel** · **PostgreSQL** · **Docker Compose**
Deadline: 7 days. Minimum to pass: 60/100 base, **≥6/15 backend**, **≥6/15 frontend**.

> Priority order: (1) eliminatory requirements, (2) one solid end-to-end flow, (3) scored features, (4) bonus.
> Anything not finished must be documented honestly in the README — incomplete ≠ invalid.

---

## Phase 0 — Setup & Scaffolding

- [x] ~~Create repo, `main` branch, `.gitignore` (vendor, node_modules, `.env`, storage)~~ [19/09/2026]
- [x] ~~Decide monorepo layout: `/backend`, `/frontend`, `/docs`, `docker-compose.yml`, `README.md`~~ [19/09/2026]
- [ ] Commit early and often — commit history is evaluated as evidence of organization
- [ ] Scaffold Laravel API (`composer create-project laravel/laravel backend`)
- [ ] Scaffold React + TS (`npm create vite@latest frontend -- --template react-ts`)
- [ ] `docker-compose.yml` with 3 services: `db` (postgres), `api` (php-fpm/nginx or artisan serve), `web` (vite/nginx)
- [ ] Dockerfiles for api and web; named volume for postgres data
- [ ] `.env.example` for backend **and** frontend — no real secrets anywhere in git
- [ ] Verify `docker compose up` brings all three up and API reaches Postgres
- [ ] Entrypoint runs `migrate --force` (+ seed) automatically on boot
- [ ] Configure CORS in Laravel so the frontend origin can call the API

---

## Backend (Laravel) — 15 pts + drives the 25 pts integration score

### Data model & database
- [ ] Migration `solicitacoes`:
  - [ ] `id` (PK)
  - [ ] `protocolo` — **unique**, generated by the app
  - [ ] `nome_solicitante`
  - [ ] `categoria` — CONSULTA | EXAME | VACINACAO | OUTRO
  - [ ] `prioridade` — BAIXA | MEDIA | ALTA | URGENTE
  - [ ] `status` — RECEBIDA | EM_ANALISE | AGENDADA | CONCLUIDA | CANCELADA
  - [ ] `descricao`
  - [ ] `justificativa_prioridade` — nullable, required when URGENTE
  - [ ] `data_criacao` / `data_atualizacao` (timestamps)
- [ ] Decide & justify enum strategy (PG enum vs varchar + CHECK constraint) in README
- [ ] NOT NULL constraints on required fields; unique index on `protocolo`
- [ ] Indexes on `status`, `categoria`, `prioridade` (they back the list filters)
- [ ] `Solicitacao` Eloquent model with casts + PHP enums for the four value sets
- [ ] Factory + Seeder with **fictional data only** (use `fake()->name()` in pt_BR)

### Business rules (must live outside the controller)
- [ ] New records always start as `RECEBIDA`
- [ ] Unique `protocolo` generated automatically (e.g. `SOL-2026-000123`) — handle collisions
- [ ] `justificativa_prioridade` required when `prioridade = URGENTE`
- [ ] `data_criacao` set on insert; `data_atualizacao` touched on every update
- [ ] Central, testable status transition machine:
  - RECEBIDA → EM_ANALISE, CANCELADA
  - EM_ANALISE → AGENDADA, CANCELADA
  - AGENDADA → CONCLUIDA, CANCELADA
  - CONCLUIDA → *(final)*
  - CANCELADA → *(final)*
- [ ] Invalid transition throws a domain exception → 409 (or 422) with a clear message

### API REST
- [ ] `POST   /api/v1/solicitacoes` — create
- [ ] `GET    /api/v1/solicitacoes` — paginated list + filters `status`, `categoria`, `prioridade`
- [ ] `GET    /api/v1/solicitacoes/{id}` — detail (404 when missing)
- [ ] `PATCH  /api/v1/solicitacoes/{id}/status` — status update respecting transitions
- [ ] (optional) `GET /api/v1/solicitacoes/summary` — counts by status/priority for the dashboard
- [ ] Form Requests for create and status update (validation lives there, not in the controller)
- [ ] API Resources for output — stable, documented response shape
- [ ] Centralized exception handler: consistent JSON error envelope, no stack traces or internal details leaked
- [ ] Correct HTTP codes: 201, 200, 404, 422, 409, 500

### Architecture
- [ ] Thin controllers → Service/Action layer for business rules
- [ ] Dependency injection; transactions where a write spans more than one step
- [ ] Organize by domain (e.g. `app/Domain/Solicitacoes/...`) — only abstractions that earn their keep
- [ ] Keep every architectural decision explainable in the interview

---

## Frontend (React + TypeScript) — 15 pts

### Setup
- [ ] Routing (React Router): `/` dashboard, `/solicitacoes`, `/solicitacoes/nova`, `/solicitacoes/:id`
- [ ] Single HTTP client module (axios/fetch wrapper) reading `VITE_API_URL` from env
- [ ] `src/types/` — TS types/enums mirroring the API contract, **no stray `any`**
- [ ] Organize by feature/domain: `features/solicitacoes/{api,components,hooks,pages}`
- [ ] Pick async-state approach (TanStack Query or custom hooks) and justify it in README

### Screens
- [ ] **Dashboard** — summary of solicitações by status and/or priority
- [ ] **List** — paginated table/cards + filters by status, categoria, prioridade (filters in URL query params)
- [ ] **Create form** — client-side validation, `justificativa_prioridade` shown/required only when URGENTE
- [ ] **Detail** — full record + protocol + timestamps
- [ ] **Status action** — only offers transitions valid for the current status; confirmation before cancel
- [ ] Surface backend 422 field errors next to the right inputs

### Quality
- [ ] Loading, success, empty and error states on every async view
- [ ] Responsive layout (mobile first, works at ~375px)
- [ ] Semantic HTML, labels tied to inputs, keyboard navigation, focus states, `aria-live` for feedback
- [ ] Reusable presentational components (StatusBadge, PriorityBadge, Pagination, EmptyState)

---

## Tests — 15 pts

- [ ] Backend (PHPUnit/Pest) — behavior scenarios, not line coverage:
  - [ ] Creating a solicitação returns status RECEBIDA + a unique protocolo
  - [ ] URGENTE without justificativa → 422
  - [ ] Valid transition (RECEBIDA → EM_ANALISE) succeeds
  - [ ] Invalid transition (CONCLUIDA → EM_ANALISE) is rejected
  - [ ] List filters return the right subset
- [ ] Use a separate test database / `RefreshDatabase` — deterministic, no real data
- [ ] Frontend: at least one meaningful test (Vitest + React Testing Library) — e.g. the form shows the justification field and blocks submit when URGENTE is selected without it
- [ ] Document exactly how to run both suites in the README

---

## Validation, Security & Privacy — 10 pts

- [ ] Strict validation on every input (types, enum membership, max lengths)
- [ ] No credentials, tokens or passwords committed — `.env` ignored, `.env.example` present
- [ ] Fictional data only; no real medical or personal information
- [ ] Error responses reveal nothing internal (no SQL, no file paths)
- [ ] If auth is added: Sanctum + documented profiles/policies

---

## Documentation & Delivery — 5 pts

- [ ] `docs/openapi.yaml` — endpoints, params/filters, request bodies, success responses, validation errors, HTTP codes
- [ ] (optional) Swagger UI served from the compose stack
- [ ] **README** must include:
  - [ ] Technologies and versions (React, TypeScript, PHP, Laravel, PostgreSQL)
  - [ ] Full run instructions: compose up, migrations, seeders, ports, env vars
  - [ ] Architectural decisions, domain organization, frontend↔API contract, why each abstraction exists
  - [ ] What is implemented, what is not, known limitations, any manual step needed
  - [ ] How to run the tests and where the OpenAPI spec lives
  - [ ] **AI tool usage** — which tools, in which parts of the project (mandatory)
- [ ] Final check: fresh `git clone` → `docker compose up` → app works with zero manual fixes

---

## Bonus (up to +20) — only after the above is solid

- [ ] Simple auth/authorization with distinct profiles + policies
- [ ] `/api/health` health check including the Postgres connection
- [ ] Migrations + seeders run automatically on container start
- [ ] Structured logs with a request correlation ID
- [ ] CI pipeline (GitHub Actions): lint + tests + build for both sides
- [ ] Architecture doc with a simple diagram + decision log + how this module could evolve into domain services
- [ ] Extra accessibility / UX polish
- [ ] Rich seed data so evaluators see a populated app immediately
- [ ] Justified use of events/queues (e.g. notification on URGENTE) with failure handling
- [ ] Lint/format: Pint or PHP-CS-Fixer + ESLint + Prettier

---

## Suggested day plan

| Day | Focus |
|-----|-------|
| 1 | Repo, Docker Compose, Laravel + React scaffolding, DB connection working |
| 2 | Migration, model, factory/seeder, protocol generation, transition service |
| 3 | Endpoints + Form Requests + Resources + exception handling |
| 4 | Backend tests + OpenAPI spec |
| 5 | Frontend: HTTP client, types, list + filters + pagination |
| 6 | Frontend: create form, detail, status action, all async states, frontend test |
| 7 | Accessibility/responsive pass, README, bonus items, clean-clone verification |
